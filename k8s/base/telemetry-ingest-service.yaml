apiVersion: v1
kind: Service
metadata:
  name: telemetry-ingest
  namespace: fleet-management
spec:
  selector:
    app: telemetry-ingest
  ports:
    - name: http
      port: 80
      targetPort: http
